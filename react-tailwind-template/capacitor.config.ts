import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'react-tailwind-template',
  webDir: 'dist'
};

export default config;
